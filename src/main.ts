import './style.scss';
import 'aos/dist/aos.css';
import Aos from 'aos';

document.addEventListener('DOMContentLoaded', () => {
  console.log('typescript is rinnung');
  Aos.init({
    once: false,
  });
});
